# orchestrator 使用留痕（usage-log）

> 追加式记录四类：`TRIGGER OK`（触发正常）/ `TRIGGER MISS`（触发漏判，记疑似原因）/ `LOAD FAIL`（加载失败）/ `EXEC POOR`（执行质量差，记问题与用户反应）。TRIGGER MISS 与 EXEC POOR 强制升级为 5-Why 复盘（现象→根因≥2层→改进项）。

（暂无记录）
